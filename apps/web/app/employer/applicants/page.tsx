'use client';

/** Applicants — sidebar section, org-wide across every job. Auth guard/shell come from app/employer/layout.tsx. */
import EmployerApplicants from '@/components/EmployerApplicants';

export default function EmployerApplicantsPage() {
  return <EmployerApplicants />;
}
