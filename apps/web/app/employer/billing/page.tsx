'use client';

/** Billing documents — sidebar-adjacent, linked from Settings. Auth guard/shell come from app/employer/layout.tsx. */
import EmployerBillingDocuments from '@/components/EmployerBillingDocuments';

export default function EmployerBillingPage() {
  return <EmployerBillingDocuments />;
}
