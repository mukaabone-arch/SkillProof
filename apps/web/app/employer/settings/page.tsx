'use client';

/** Settings — sidebar section (stub, see EmployerSettings). Auth guard/shell come from app/employer/layout.tsx. */
import EmployerSettings from '@/components/EmployerSettings';

export default function EmployerSettingsPage() {
  return <EmployerSettings />;
}
